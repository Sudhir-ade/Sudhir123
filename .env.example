
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/rfpdb

# OpenAI
OPENAI_API_KEY=sk-...

# SMTP for sending (example)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=username
SMTP_PASS=password
FROM_EMAIL=procurements@example.com

# Inbound webhook secret (optional)
INBOUND_SECRET=changeme

PORT=4000
